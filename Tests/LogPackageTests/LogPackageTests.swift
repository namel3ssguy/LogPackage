    import XCTest
    @testable import LogPackage

    final class LogPackageTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
//            XCTAssertEqual(LogPackage.logDefault(), "Hello there")
        }
    }
